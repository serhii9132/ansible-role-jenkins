---
- name: Install prerequisite packages
  loop: "{{ __jenkins__dependencies }}"
  ansible.builtin.apt:
    update_cache: true
    name: "{{ item }}"

- name: Add the Jenkins repository with embedded key
  ansible.builtin.deb822_repository:
    name: jenkins
    types: ["deb"]
    uris: "{{ __jenkins_base_url }}"
    suites: "binary/"
    signed_by: "{{ __jenkins_gpg_key_url }}"
    enabled: true

- name: Install packages
  loop: "{{ __jenkins_packages }}"
  ansible.builtin.apt:
    update_cache: true
    name: "{{ item }}"
  notify: Start Jenkins service
